import { Registry } from '@/services/registry/interface';
import { Dispatch, SetStateAction } from 'react';

export interface RegistryLocationsProps {
  registry?: Registry;
}

export interface HeaderProps {
  registry?: Registry;
}
